
/*#include <stdio.h>*/
#ifndef __STATS_H__
#define __STATS_H__

int print_array(unsigned char t[SIZE]);
/**
 * This function takes an array as input, prints the array to the screen
 */
 
int find_minimum(unsigned char t[SIZE]);
/**
 * MIN(x, y) => (((x) < (y)) ? (x) : (y))
 * This function takes an array as input, In this way, it minimum the first
 * element and compares the next element with the first element.
 * If the next element is smaller than the first element, the displacement 
 * takes place.
 */

int  find_maximum(unsigned char t[SIZE]);
/**
 * MAX(x, y) => (((x) > (y)) ? (x) : (y))
 * This function takes an array as input, In this way, In order to maximum
 * the first element and compare the second element with this element. 
 * If the next element is larger than the first element, the displacement 
 * takes place.
 */

int find_mean(unsigned char t[SIZE]);
/**
 * This function takes an array as input,Calculates the average of array numbers. By first calculating the sum of the numbers and then dividing it by the size of the array.
 */
 
int sort(unsigned char t[SIZE]);
 /**
 * This function takes an array as input,Examines the elements of the array in order
 * and sorts them in descending order.
 * Uses selection sort to sort array elements.
 */


#endif /* __STATS_H__ */
